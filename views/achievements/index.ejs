<div class="page-heading">
    <section class="section">
        <div class="card">
            <div class="card-header">
                <a href="/achievements/create" class="btn btn-primary">Tambah +</a>
            </div>
            <div class="card-body">
               
                <%- include('../partials/message.ejs') %>
                
                <div class="table-responsive">
                    <table class="table" id="table1">
                        <thead>
                            <tr>
                                <th>No</th>
                                <th>Nama Kegiatan</th>
                                <th>Nama Peserta</th>
                                <th>Peringkat</th>
                                <th>Tahun</th>
                                <th>Dibuat</th>
                                <th>Diedit</th>
                                <th>Aksi</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% achievements.forEach((achievement, index) => { %>
                               <tr>
                                    <td><%= index + 1 %></td>
                                    <td><%= achievement.activity_name %></td>
                                    <td><%= achievement.name %></td>
                                    <td><%= achievement.rank %></td>
                                    <td><%= achievement.year %></td>
                                    <td><%= achievement.createdAt %></td>
                                    <td><%= achievement.updatedAt %></td>
                                    <td>
                                        <a href="/achievements/<%= achievement._id %>/edit"
                                            class="btn btn-warning text-white"><i class="bi bi-pencil-fill"></i></a>
                                        <button class="btn btn-danger delete-confirm" data-action="/achievements/<%= achievement._id %>?_method=DELETE"><i class="bi bi-trash3-fill"></i></button>
                                    </td>
                               </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </section>
</div>