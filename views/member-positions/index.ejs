<div class="page-heading">
    <section class="section">
        <div class="card">
            <div class="card-header">
                <a href="/member-positions/create" class="btn btn-primary">Tambah +</a>
            </div>
            <div class="card-body">
               
                <%- include('../partials/message.ejs') %>
                
                <div class="table-responsive">
                    <table class="table" id="table1">
                        <thead>
                            <tr>
                                <th>No</th>
                                <th>Nama Jabatan Pengurus</th>
                                <th>Dibuat</th>
                                <th>Diedit</th>
                                <th>Aksi</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% memberPositions.forEach((memberPosition, index) => { %>
                               <tr>
                                    <td><%= index + 1 %></td>
                                    <td><%= memberPosition.name %></td>
                                    <td><%= memberPosition.createdAt %></td>
                                    <td><%= memberPosition.updatedAt %></td>
                                    <td>
                                        <a href="/member-positions/<%= memberPosition._id %>/edit"
                                            class="btn btn-warning text-white"><i class="bi bi-pencil-fill"></i></a>
                                        <button class="btn btn-danger delete-confirm" data-action="/member-positions/<%= memberPosition._id %>?_method=DELETE"><i class="bi bi-trash3-fill"></i></button>
                                    </td>
                               </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </section>
</div>