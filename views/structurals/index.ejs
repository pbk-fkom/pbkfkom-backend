<div class="page-heading">
    <section class="section">
        <div class="card">
            <div class="card-header">
                <a href="/structurals/create" class="btn btn-primary">Tambah +</a>
            </div>
            <div class="card-body">
               
                <%- include('../partials/message.ejs') %>
                
                <div class="table-responsive">
                    <table class="table" id="table1">
                        <thead>
                            <tr>
                                <th>No</th>
                                <th>Nama Struktural</th>
                                <th>Deskripsi</th>
                                <th>Dibuat</th>
                                <th>Diedit</th>
                                <th>Aksi</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% structurals.forEach((structural, index) => { %>
                               <tr>
                                    <td><%= index + 1 %></td>
                                    <td><%= structural.name %></td>
                                    <td><%= structural.description %></td>
                                    <td><%= structural.createdAt %></td>
                                    <td><%= structural.updatedAt %></td>
                                    <td>
                                        <a href="/structurals/<%= structural._id %>/edit"
                                            class="btn btn-warning text-white"><i class="bi bi-pencil-fill"></i></a>
                                        <button class="btn btn-danger delete-confirm" data-action="/structurals/<%= structural._id %>?_method=DELETE"><i class="bi bi-trash3-fill"></i></button>
                                    </td>
                               </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </section>
</div>